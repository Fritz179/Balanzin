class Game extends Layer {
  constructor() {
    super()
  }

  // spawn(entity) {
  //   if (entity instanceof Entity) this.addChild(entity)
  //   else if (entity instanceof Layer) console.warn('WIP!');
  //   else console.error('entity is not instanceof Enrtity: ', entity);
  // }
}
